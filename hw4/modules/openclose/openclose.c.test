#include<stdlib.h>
#include <sys/stat.h>
#include <assert.h>
#include <stdio.h>
#include <fcntl.h>


int main() {
	int fd, i;

	for(i = 0; i < 5; i++) {
		fd = open("/dev/openclose4", O_RDWR);
		if(fd < 0) {
			perror("sahne");
			continue;
		}
		printf("Beste leben %d\n", i);
	}

	
}
