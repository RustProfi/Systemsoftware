#include <stdlib.h>
#include <sys/stat.h>
#include <assert.h>
#include <stdio.h>
#include <string.h>
#include <fcntl.h>
#include <unistd.h>
#include <errno.h>
#include <time.h>
#include <pthread.h>
#include <signal.h>

int fd;
ssize_t rd;
char buf[10000];
char * str = NULL;

int main() {    
    fd = open("/proc/kmsg", O_RDWR);
    if(fd < 0){
        perror("open: ");
        return 1;
    }
        rd = read(fd, buf,10000);
        str = strstr(buf, "Tasklet grp5");
        if(str != NULL){
		return 0;
	} 
    
    return 1;
}
